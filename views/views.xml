<odoo>
  <data>
    <!-- Vue search -->
    <!--
    <record model="ir.ui.view" id="[model_name]_search_view">
        <field name="name">[model.name].search</field>
        <field name="model">[model.name]</field>
        <field name="arch" type="xml">
            <search string="[Nom du modèle]">
                <group>
                    <field name="[champ1]" select="1"/>
                    <field name="[champ2]" select="1"/>
                    <field name="[...]" select="1"/>
                </group>
            </search>
        </field>
    </record>
    -->
    
    <!-- Vue tree -->
    <!--
    <record model="ir.ui.view" id="[model_name]_tree_view">
      <field name="name">[model.name].tree</field>
      <field name="model">[model.name]</field>
      <field name="arch" type="xml">
        <tree string="[Nom du modèle]">
            <field name="[champ1]"/>
	        <field name="[champ2]"/>
	        <field name="[...]"/>
        </tree>
      </field>
    </record>
    -->

    <!-- Vue form -->
    <!--
    <record model="ir.ui.view" id="[model_name]_form_view">
        <field name="name">[model.name].form</field>
        <field name="model">[model.name]</field>
        <field name="arch" type="xml">
            <form string="[Nom du modèle]">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <span class="oe_inline">[Nom du modèle]</span>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="[champ1]"/>
                            <field name="[champ2]"/>
                            <field name="[...]"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    -->  
    
    <!-- Vue form (exemple héritage) -->
    <!--
    <record model="ir.ui.view" id="[inherited_model_name]_form_view_[module_name]">
        <field name="name">[inherited.model.name].form.[module.name]</field>
        <field name="model">[inherited.model.name]</field>
        <field name="inherit_id" ref="[src_view_id]" />
        <field name="priority" eval="99"/>
        <field name="arch" type="xml">
            <field name="[base_field_name]" position="after">
             	 <field name="[custom_field_name]"/>
            </field>
        </field>
    </record>
    -->
    
    <!-- actions opening views on models -->
    <!-- 
    <record id="[model_name]_action" model="ir.actions.act_window">
        <field name="name">[Nom du modèle]</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">[model.name]</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- Top menu item (Niveau 1) -->
    <!--
    <menuitem name="[Nom menu]" id="[module_name].menu_root"/>
    -->
    <!-- menu categories (Niveau 2) -->
    <!--
    <menuitem name="[Nom Menu 1]" id="[module_name].[menu_name_1]" parent="[module_name].menu_root"/>
    <menuitem name="[Nom Menu 2]" id="[module_name].[menu_name_2]" parent="[module_name].menu_root"/>
    -->
    <!-- actions (Niveau 3) -->
    <!--
    <menuitem name="[Nom Menu 1-1]" id="[module_name].[menu_name_1]" parent="[module_name].[menu_name_1]"
              action="[module_name].[action_id]"/>
    -->
  </data>
</odoo>